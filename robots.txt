# console.aismartcontent.io — robots.txt
#
# Este subdominio es la APP autenticada (login + workspace). El SEO marketing
# vive en aismartcontent.io (Framer), que tiene su propio robots/sitemap.
# Aquí bloqueamos toda indexación para evitar que Google muestre pantallas
# de login o páginas internas en resultados.

User-agent: *
Disallow: /

# Permitir scrapers de previews (WhatsApp/Slack/LinkedIn/Twitter) que leen og:tags
# vía User-agent específico — el `Disallow: /` bloquea crawlers de búsqueda, no
# evita que estos bots lean meta tags al compartir un link.
