{
  "name": "ai-smart-content",
  "version": "0.1.0",
  "private": true,
  "description": "AI Smart Content — frontend SPA + Netlify Functions (smoke tests against Supabase + ai-engine)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:ci": "eslint . --max-warnings 630",
    "audit:isolation": "node scripts/audit-org-isolation.mjs",
    "build:minify": "node scripts/build-minify.mjs"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.0",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.25.0",
    "@supabase/supabase-js": "^2.45.0",
    "eslint": "^10.6.0",
    "vitest": "^2.1.0"
  }
}
